export type Mode =
  | "primary"
  | "secondary"
  | "success"
  | "danger"
  | "warning"
  | "info";

export enum BootstrapModes {
  primary = "primary",
  secondary = "secondary",
  success = "success",
  danger = "danger",
  warning = "warning",
  info = "info",
}
